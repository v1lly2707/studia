<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Application">
                <MenuItem Header="Generate data" Click="GenerateData_Click"/>
                <MenuItem Header="Version" Click="Version_Click"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
                <MenuItem Header="Zapytanie1" Click="Zapytanie_1"/>
                <MenuItem Header="Zapytanie2" Click="Zapytanie_2"/>
                <MenuItem Header="Serialize" Click="Serialize_Click"/>
                <MenuItem Header="Deserialize" Click="Deserialize_Click"/>
                <MenuItem Header="XPath" Click="XPath_Click"/>
                <MenuItem Header="XHTML" Click="XHTML_Click"/>
            </MenuItem>
        </Menu>

        <TreeView x:Name="ObjectTreeView" Grid.Row="1" Grid.Column="0" SelectedItemChanged="ObjectTreeView_SelectedItemChanged">
            <TreeView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Create" Click="Create_Click"/>
                    <MenuItem Header="Delete" Click="Delete_Click"/>
                </ContextMenu>
            </TreeView.ContextMenu>
        </TreeView>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ScrollViewer Height="100" VerticalScrollBarVisibility="Auto" Grid.Row="0">
                <TextBlock x:Name="ObjectDetailsTextBlock" TextWrapping="Wrap"/>
            </ScrollViewer>

            <ToolBarTray Grid.Row="1" Margin="5">
                <ToolBar>
                    <TextBlock Text="Wybierz pole:" Margin="0,0,5,0"/>
                    <ComboBox x:Name="PropertyComboBox"
                              Width="200"
                              DropDownOpened="PropertyComboBox_DropDownOpened"/>
                    <TextBlock Text="Szukaj:" Margin="0,0,5,0"/>
                    <TextBox x:Name="SearchTextBox" Width="150" Margin="0,0,10,0" KeyDown="SearchTextBox_KeyDown"/>
                </ToolBar>
            </ToolBarTray>

            <DataGrid x:Name="ModelDataGrid"
                AutoGenerateColumns="True"
                CanUserAddRows="True"
                CanUserDeleteRows="True"
                CanUserSortColumns="True"
                IsReadOnly="False"
                Margin="0"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                Grid.Row="2">
                <DataGrid.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Dodaj" Click="Create_Data_Click"/>
                        <MenuItem Header="Usuń" Click="Delete_Data_Click"/>
                        <MenuItem Header="Sortuj" Click="Sort_Click"/>
                    </ContextMenu>
                </DataGrid.ContextMenu>
            </DataGrid>
        </Grid>

    </Grid>
</Window>
